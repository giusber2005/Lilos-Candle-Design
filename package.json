{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "sh -c 'set -a && . ./.env && set +a && DATABASE_URL=file:$(pwd)/dev.db PORT=${API_PORT:-3001} artifacts/api-server/node_modules/.bin/tsx artifacts/api-server/src/index.ts'",
    "dev:web": "sh -c 'set -a && . ./.env && set +a && PORT=5173 BASE_PATH=/ artifacts/liloscandle/node_modules/.bin/vite --config artifacts/liloscandle/vite.config.ts'",
    "db:push": "sh -c 'set -a && . ./.env && set +a && DATABASE_URL=file:$(pwd)/dev.db pnpm --filter @workspace/db run push'",
    "db:seed": "sh -c 'set -a && . ./.env && set +a && DATABASE_URL=file:$(pwd)/dev.db pnpm --filter @workspace/db run seed-content'"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
